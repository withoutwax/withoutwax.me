import React from 'react';
import Layout from '../components/Layout';

export default () => (
    <Layout>
        <div style={{ color: `teal`, fontSize: `72px` }}>
            Hello World!
        </div>
    </Layout>
);